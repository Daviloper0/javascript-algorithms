//In this challenge there is a function that recieves a value in celsius and have to return it in fahrenheit. I was supposed to use a variable, so I did it.

function convertToF(celsius) {
    let fahrenheit = celsius * (9/5) + 32;
    return fahrenheit;
  }
  
  convertToF(30);